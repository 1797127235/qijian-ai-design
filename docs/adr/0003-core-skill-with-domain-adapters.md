# 采用通用核心 skill 与场景适配器

平台的初步场景包含家装、工装和景观，但三者的专业约束不同。我们采用通用核心 skill 加场景适配器：核心负责理解项目、确认方向、管理资产、记录决策和处理交付；家装、工装和景观适配器负责各自的领域字段、约束、质量检查和提案模板。这样可以复用流程纪律，又不会把家装假设硬编码成所有空间设计的模型。

**Status:** accepted

**Considered Options**

- 直接把 skill 改成家装专用：短期更快，但会阻碍工装和景观扩展。
- 维持完全通用、不定义场景适配器：表面灵活，但无法表达专业约束。

