# MVP 先落地 Artifact 状态，再接真实 AI

当前界面已经可以模拟 Brief、项目理解卡和设计方向，但状态仍然只存在于 React 内存中。下一阶段先实现可保存、可恢复、可追溯的 Artifact 状态，再接入真实 AI；这样每次生成都有明确的输入版本、项目归属和设计决策，不会把模型演示误当成产品闭环。

**Status:** accepted

**Considered Options**

- 先接图像或视觉模型：能快速展示效果，但无法验证项目持续性和版本关系。
- 先做完整后端和账号系统：边界清晰，但会在尚未验证状态模型前引入过多基础设施。

